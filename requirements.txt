# VoCoType Flutter Python runtime dependencies
# 注：pyproject.toml 为标准安装入口；本文件保留用于 pinned 安装

# 音频采集和处理
sounddevice==0.5.2
librosa==0.11.0
soundfile==0.13.1

# 语音识别核心
funasr_onnx==0.4.1
jieba==0.42.1

# 模型下载 (推荐)
modelscope==1.30.0

# 可选：
# - Rime 支持通过 pyproject.toml 的 [full] / [rime] extra 安装
